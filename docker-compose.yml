services:
  app:
    image: ghcr.io/nov1ce-lee/mc-website:latest
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:/app/prisma/data/dev.db
      NEXTAUTH_URL: http://nov1ce.top:3000
      NEXTAUTH_SECRET: aB3dE5fG7hJ9kL1mN2oP4qR6sT8uV0wX
      MC_SERVER_IP: 114.67.238.112:34196
    volumes:
      - sqlite_data:/app/prisma/data
    command: >
      sh -c "npx prisma db push && npm start"

volumes:
  sqlite_data:
